var fixtureArray = [Math.random(), Math.random(), Math.random(), Math.random()];

var dataFixture = {
  address: "127.0.0.1",
  family: "IPv4",
  path: "/muse/elements/beta_session_score",
  port: 53146,
  size: 60,
  values: fixtureArray
}

var focusFixture = {
  address: "127.0.0.1",
  family: "IPv4",
  path: "/muse/elements/experimental/concentration",
  port: 62924,
  raw: ArrayBuffer,
  size: 52,
  values: Math.random()
}

var testSaveObject = {

}

// new THRE
